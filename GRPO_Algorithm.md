***

# 🧮 算法心脏：为什么 GRPO 正在取代 PPO？

本文档深度拆解了具身智能 VLA（视觉-语言-动作）模型在强化学习阶段的核心算法演进：从经典的 **PPO (近端策略优化)** 到为大模型量身定制的 **GRPO (群组相对策略优化)**。

## 🕳️ 一、 痛点：PPO 的“显存黑洞”与“昂贵的评估师”

在经典的 PPO 算法中，为了判断机器人做出的动作（Action）到底是好是坏，算法采用了 **Actor-Critic（演员-评论家）** 架构：

* **Actor (演员/主厨)**：负责看画面并做动作的网络。
* **Critic (评论家/评估师)**：负责预测当前状态下“预期能拿多少平均分”的网络。

**致命瓶颈：**
在 VLA 模型中，Actor 通常是一个拥有数十亿参数（如 7B）的庞然大物。为了让 Critic 也能看懂同样复杂的视觉画面并做出准确预测，Critic 通常也需要是一个同样规模（7B）的大模型。
这就意味着你的显卡里同时塞进了两个 7B 模型，加上训练所需的梯度和优化器状态，普通设备的显存（VRAM）会瞬间溢出（OOM）。

## ✨ 二、 破局：GRPO 的“群组相对比较”魔法

**GRPO 的核心创新：彻底开除 Critic（评估师）网络！**

既然没有评估师来预测“预期平均分”，GRPO 采用了极其聪明且符合直觉的策略——**同台竞技，不请外援**。

面对同一个厨房画面（Observation），算法不再只让模型输出 1 个动作，而是让模型的分支**同时构思并输出一组（Group）动作方案**（例如 5 个不同的动作）。

当这 5 个动作在物理环境（或模拟器）中分别得到真实的得分后，算法直接进行内部比较：
1.  计算这 5 个动作的**真实平均分**。
2.  **高于平均分**的动作 $\rightarrow$ 判定为好动作，增加其未来被输出的概率。
3.  **低于平均分**的动作 $\rightarrow$ 判定为坏动作，降低其未来被输出的概率。

## 📐 三、 核心数学公式 (Advantage 计算)

在强化学习中，衡量一个动作含金量的指标叫做**优势函数 (Advantage)**，记为 $A_i$。GRPO 计算优势值的公式非常优雅且极度节省算力：

$$A_i = \frac{R_i - \text{mean}(R)}{\text{std}(R)}$$

* $R_i$：当前第 $i$ 个动作在环境中获得的真实得分。
* $\text{mean}(R)$：同批次（Group）所有动作得分的平均值。
* $\text{std}(R)$：同批次得分的标准差（用于数值平滑缩放，防止参数更新时用力过猛）。

通过这个公式计算出 $A_i$ 后，算法会将其乘到大模型的参数梯度上。$A_i$ 为正，参数向该动作靠拢；$A_i$ 为负，参数远离该动作。

## 🚀 四、 GRPO 带来的两大革命性优势

1.  **显存开销直接减半**：由于彻底砍掉了同等体量的 Critic 网络，普通实验室和个人开发者也能在一台多卡服务器上微调庞大的 VLA 模型。
2.  **自我进化更稳定**：不再依赖 Critic 虚无缥缈且可能产生误差的“预测分数”，而是实打实地让模型“自己跟自己比”。只要比同批次的其他动作表现好，模型就能获得明确的进步方向。

***
